function handleCTAClick(action) {
  alert("You clicked the CTA: " + action);
  // Track analytics event, or redirect as needed
}
function handleShop(product) {
  alert("Shop action for " + product + " ✓");
  // Track or redirect to product purchase flow
}
