function handleCTAClickH(action) {
  alert("You clicked the CTA: " + action);
  // Add analytics, personalized sport redirect, etc.
}
function handleShopH(product) {
  alert("Shop: " + product);
  // Track or send to product page
}
function handleCompare(product) {
  alert("Compare: " + product + " with other Wilson gear.");
  // Pop up modal or highlight compare features
}
function handleTryNow() {
  alert("Try Wilson best-sellers risk-free.");
}
function handleBundle() {
  alert("Bundle & Save offer selected.");
}
